import 'package:flutter/material.dart';

class OpponendCard extends StatelessWidget {
  const OpponendCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}